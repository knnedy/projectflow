version: "2"
sql:
  - engine: "postgresql"
    queries: "./internal/sql/queries/*.sql"
    schema: "./internal/sql/schema/*.sql"
    gen:
      go:
        package: "repository"
        out: "./internal/repository"
        sql_package: "pgx/v5"
